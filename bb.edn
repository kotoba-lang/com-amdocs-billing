{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([amdocs_billing.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'amdocs_billing.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
